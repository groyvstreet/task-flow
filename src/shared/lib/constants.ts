export const STORAGE_KEYS = {
    TASKS: 'tasks',
    ACTIONS: 'actions',
    ATTACHMENTS: 'attachments',
    THEME: 'theme',
    DELETED_TASK_IDS: 'deleted_task_ids',
    DELETED_ATTACHMENT_IDS: 'deleted_attachment_ids',
    HISTORY_CLEAR_PENDING: 'history_clear_pending',
} as const;

export const NOTIFICATION_MINUTES_BEFORE = 30;
export const DEMO_NOTIFICATION_DELAY_SECONDS = 10;
